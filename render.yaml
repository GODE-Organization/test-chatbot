services:
  - type: web
    name: telegram-bot
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: BOT_USE_WEBHOOK
        value: "false"
